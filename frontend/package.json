{
  "name": "nightwave-club-frontend",
  "version": "1.0.0",
  "description": "Nightwave Club - Digital Club Store Frontend",
  "private": true,
  "scripts": {
    "build": "echo 'Static site - no build required'",
    "lint": "echo 'No linter configured'",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
